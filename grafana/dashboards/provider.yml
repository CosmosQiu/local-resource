# Grafana dashboard provisioning
apiVersion: 1

providers:
  - name: "ARH Dashboards"
    orgId: 1
    folder: "AI Resource Hub"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
