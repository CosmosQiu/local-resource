# =============================================================================
# AI Resource Hub — Backend Dependencies
# Python 3.14 — packages must have cp314 pre-built wheels
# =============================================================================

# -------------------- FastAPI Core --------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.10.0
pydantic-settings>=2.5.0

# -------------------- Database --------------------
# psycopg 3 replaces both asyncpg (async) and psycopg2 (sync)
sqlalchemy[asyncio]>=2.0.35
psycopg[binary,pool]>=3.2.10
alembic>=1.13.0

# -------------------- Auth & Security --------------------
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pycasbin>=2.8.0
cryptography>=43.0.0

# -------------------- Async Tasks --------------------
celery[redis]>=5.4.0
redis>=5.0.0

# -------------------- HTTP Client (GPUStack API, cookie fetching) --------------------
httpx>=0.27.0
aiohttp>=3.10.0

# -------------------- Ansible Integration --------------------
ansible-runner>=2.4.0

# -------------------- Pydantic extras --------------------
email-validator>=2.0.0

# -------------------- Dev & Debug --------------------
python-dotenv>=1.0.0

# -------------------- Testing --------------------
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
aiosqlite>=0.20.0
